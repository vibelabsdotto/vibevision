# deps
node_modules/
.pnp
.pnp.js

# next
.next/
out/
next-env.d.ts

# pocketbase runtime data
backend/pocketbase/data/
backend/pocketbase/pb_data/
backend/pocketbase/pocketbase
backend/pocketbase/*.zip
backend/pocketbase/Dockerfile.local

# env & secrets
.env
.env.*
!.env.example
secrets/

# os
.DS_Store
Thumbs.db

# hermes agent artifacts
.hermes/
.agents/
skills-lock.json

# ts
*.tsbuildinfo

# pocketbase local artifacts (reproducible / regenerable — never commit)
backend/pocketbase/bin/
backend/pocketbase/pb_migrations/

# CLI state (instance + stored superuser API keys — never commit)
.vv/

# logs
npm-debug.log*
yarn-error.log*
pnpm-debug.log*
